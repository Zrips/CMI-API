package com.Zrips.CMI.Modules.ChatFilter;

import org.bukkit.event.Listener;
import org.bukkit.event.block.SignChangeEvent;
import org.bukkit.event.player.AsyncPlayerChatEvent;
import org.bukkit.event.player.PlayerCommandPreprocessEvent;

import com.Zrips.CMI.CMI;

public class ChatFilterListener implements Listener {
    private CMI plugin;

    public ChatFilterListener(CMI plugin) {
    }

    private void capsFilter(AsyncPlayerChatEvent event) {
    }

    private void onChatSimpleReplace(AsyncPlayerChatEvent event) {
    }

    private void AsyncPlayerChatEvent(AsyncPlayerChatEvent event) {
    }

    private void spamFilter(AsyncPlayerChatEvent event) {
    }

    private void spamCommandFilter(PlayerCommandPreprocessEvent event) {
    }

    private void onSignChangeEventAdvertisement(SignChangeEvent event) {
    }
}
